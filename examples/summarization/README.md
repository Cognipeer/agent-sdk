# Summarization Example

Triggers summarization by starting a long session.

## Run

From the `examples/` directory:

```bash
# Set your API key
export OPENAI_API_KEY=sk-...

# Run the example
npm run example:summarization
```

Or directly:
```bash
OPENAI_API_KEY=... npx tsx summarization/summarization.ts
```
